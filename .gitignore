# Real pantry data -- never belongs in a (public) repo.
Imported Data/
pantry.db
pantry.db-wal
pantry.db-shm
pantry.db.backup-*
secret.txt
*.numbers
*.xlsx
*.xlsm
*.csv

# Build artifacts -- regenerated by `pyinstaller pantry.spec`. dist/ in
# particular can contain a built .app with the CURRENT live database baked
# in as seed data (see db.seed_database_if_missing) -- must never be
# committed.
build/
dist/

# Python / project housekeeping
venv/
__pycache__/
*.pyc
.DS_Store
timeslots_check*.html
