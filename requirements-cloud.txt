# Used by the Docker image for cloud deployment (Render) -- see Dockerfile.
# Deliberately separate from requirements.txt: pyinstaller and rumps there
# are only for building the local macOS menu-bar app, and rumps in
# particular (a Cocoa/macOS binding) won't even install on Linux.
Flask==3.0.3
Flask-WTF==1.2.2
gunicorn==23.0.0
b2sdk==2.11.0
cryptography==50.0.1
